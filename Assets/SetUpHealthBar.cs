using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetUpHealthBar : MonoBehaviour
{
    [SerializeField] private HealthBar healthBar1, healthBar2, healthBar3, healthBar4;

    private void Start()
    {
        // get player owner index and set it to healthbar1

        // add all index player to a list

        // healthBar2.SetIndexPlayer(first index of list)
        // remove first index of list

        // healthBar3.SetIndexPlayer(first index of list)
        // remove first index of list

        // healthBar4.SetIndexPlayer(first index of list)
        // remove first index of list

        Destroy(this);
    }
}
